# D4 — Scheduled send job 🚧

**Status:** blocked · **Phase:** 3 · **Blocked-by:** D3 · **Blocks:** none

## Scope (as planned, Pass 4 micro-plan)

`digest/send-job.ts` — the weekly job: for every user with `enabled=true` whose
`dayOfWeek`/`timezone` say "now," build content via `digest/content.ts`, render, send via the
existing transactional-email provider, write a `digestSends` row.

## Files

- `src/digest/send-job.ts`
- scheduler config (mechanism TBD — see blocker)

## Acceptance (as planned)

Running the job sends exactly one digest per eligible user, writes a `digestSends` row per
send, and is safe to re-run without double-sending (idempotent against `digestSends`).

## Re-spec — what actually shipped

Not started — see blocker below. 🚧 human-gated: this ticket touches external scheduling
infra, so per `dstack-yolo`'s operating contract it doesn't proceed autonomously regardless of
`risk_tolerance`.

## Blocker notes

This is `notes.md`'s open-questions-ledger **Q3** ("what triggers the weekly job — app-level
cron, or the hosting platform's scheduler?"), raised at Pass 1 and reconfirmed still-open at
both the Pass 2 and Pass 3 ledger-confirmation passes — deferred to this specific ticket rather
than guessed at during architecture. It arriving unresolved when D3 finished and D4 became
eligible isn't a gap the process missed; it's the deferred row showing up at its named target,
exactly as tracked. It's still a real infra decision needing a human call before even the
Pass-4 micro-plan can be written meaningfully — flagged `[!]` + 🚧 rather than guessed at.

---
_Marked blocked 2026-06-06 — `Dstack-Ticket: D4`._
