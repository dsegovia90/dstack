# Digest Emails — Retro (2026-06-14)

Triggered per `retro_cadence: per-phase` at the Phase 2 boundary (D1–D3 done, D4 blocked, D5
not started). Sources read: `TODO.md`, `execution-log-archive.md`, `tickets/D1.md`–`D4.md`,
`findings/*.md`, and `git log -p -- notes.md`.

## Timeline

Plan (Pass 3) assumed D2 and D3 would run in parallel once D1 landed, with both feeding D4/D5
in Phase 3. That's roughly what happened: D1 (2026-06-02→03), then D2 and D3 both picked
2026-06-03, finishing 06-04 and 06-05 respectively — genuinely parallel, one day apart, no
warm-context penalty from working them separately. D4 became eligible 06-06 and immediately
blocked on an unresolved Pass-2 open question rather than an implementation problem.

## Divergence log

- **D1** — added a `timezone` column not in the original Pass-2 schema, caught during the
  Pass-4 micro-plan rather than during Pass 2 itself. Re-specced same-commit as the code
  (2026-06-03).
- **D3** — dropped an unticketed "highlight top item" idea that had leaked into Pass-2 prose
  without ever being groomed into scope; also absorbed the `email-deliverability-review`
  finding directly into the ticket. Re-specced same-commit (2026-06-05).

Both re-specs landed in the same commit as the code that caused them — the structural bundling
described in `llm-coding-workflow.md`'s Keystone section worked exactly as intended here, with
zero gap between code and doc truth.

## Blockers

- **D4** — blocked immediately on pickup, not from an implementation dead-end but from a
  planning gap: Pass 2 left "what triggers the weekly job" as an explicit open question and it
  was never actually resolved before D4 became eligible. This is a real gap in this project's
  own Pass 2, not a process failure — worth naming plainly rather than glossing over.

## Findings

- `email-deliverability-review` — absorbed into D3. Good outcome: caught before shipping, at
  near-zero cost since it landed inside a ticket already touching that file.
- `unused-digest-column` — dismissed with a stated reason (cheap to keep, plausibly useful
  later). Reasonable call; revisit if it's still unused by the next retro.
- `rate-limit-audit` — still `status: new` as of this retro. Not a process failure by itself
  (it was found 06-13, one day before this retro, and the findings-scan pre-step will catch it
  the next time a ticket is picked) — but worth naming here so it doesn't get lost between now
  and the next pickup.

## DAG growth

None yet — the Pass-3 ticket set (D1–D5) hasn't needed a new node added mid-execution. Worth
re-checking at close-out; D4's blocker could plausibly reveal one once the scheduler decision
is made (e.g. a separate "wire up scheduler infra" ticket if the answer turns out to be
nontrivial).

## Process observations

- **Re-spec timing matches the spec's prediction closely** — both D1 and D3's re-specs were
  same-commit, which is the reliable end of the range `llm-coding-workflow.md` describes, not
  the multi-week-gap failure mode. Nothing to change here.
- **A genuinely useful pattern worth watching across future projects:** D4's blocker traces
  back to an open question from Pass 2 that was *named* but never *forced to resolution* before
  execution reached it. If this recurs on other projects, it'd suggest Pass 3 should include an
  explicit check — "are all of Pass 2's open questions resolved, or knowingly deferred with an
  owner, before generating the DAG" — rather than letting an unresolved question surface as a
  same-day blocker once a ticket becomes eligible. One occurrence isn't enough to change the
  spec on; flagging it as a candidate for `meta/process-notes.md` if a second project shows the
  same pattern.
